const a = 10;
var   b, c,123abc;
 
procedure fun1;
    if a <= 10 then
        begin
            c := b + a;
            b:2;
        end
begin
    read(b);
    while b # 0 do
        begin
            call fun1;
            write(2 * c);
            read(b);
        end
end